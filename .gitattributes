# 既定はリポジトリ内 LF・チェックアウト時も LF。
* text=auto eol=lf

# Windows でそのまま実行するものは CRLF で取り出す。
# バッチファイルは LF だと環境によって行の解釈が壊れる。
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# 触ると壊れるもの。
*.png binary
*.jpg binary
*.ico binary
*.icns binary
*.tif binary
*.webp binary
